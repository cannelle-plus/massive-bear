/*jshint expr: true*/
// var WebSocket = require('../../src/eventListener/webSocket');
// var Sessions = require('../../src/eventListener/sessions');
// var expect = require('chai').expect;

// describe('A web socket ', function() {
//     it('can be instantiated with an io and an object sessions', function() {
//     	var io = {};
//     	var sessions = new Sessions();
//         var ws = new WebSocket(io, sessions);
//         expect(ws).to.be.an('object');

//     });

//     // it('can send messages', function () {
//     // 	var msg= {};
//     //     var ws = new WebSocket();
//     //     ws.send(msg);

//     // });
// });

// describe('Given that an web socket is created', function() {
//     it('When it receives a new connection, then it is pushed to the dictionnary', function() {

//     	var sessions = {};
//     	var io = {};
//         var ws = new WebSocket(io, openedConnections);
        
        
//         expect(openedConnections[userId7]).to.be.ok;

// 	});
// });